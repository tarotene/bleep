{
  "PreToolUse": [
    {
      "matcher": "Bash|mcp__.*",
      "hooks": [
        {
          "type": "command",
          "command": "\"${CLAUDE_PLUGIN_ROOT}\"/hooks/pg-hook.sh --host=claude",
          "timeout": 20
        }
      ]
    }
  ]
}
