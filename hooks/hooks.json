{
  "PreToolUse": [
    {
      "matcher": "Bash|mcp__.*",
      "hooks": [
        {
          "type": "command",
          "command": "\"${CLAUDE_PLUGIN_ROOT}\"/hooks/bleep.sh --host=claude",
          "timeout": 20
        }
      ]
    }
  ]
}
