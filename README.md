# publish-guard

Repository scaffolding. The tool itself lands in stage A1 (`feat/cli-core`);
the full README (usage, security-boundary caveats, allowlist format) lands in
stage A4 (`feat/readme-release`).
